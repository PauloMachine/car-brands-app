import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: "100%",
    marginVertical: 10,
    padding: 10,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 50,
    color: "#fff",
  },
  viewForm: {
    width: "100%",
  },
  textError: {
    color: "#EA4335",
  },
  textLabel: {
    color: "#ffffff",
  },
});

export default styles;
